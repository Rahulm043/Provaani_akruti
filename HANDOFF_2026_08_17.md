# 🚀 Provaani Akruti Aesthetics — Production Handoff Document
**Date:** August 17, 2026  
**Status:** **100% PRODUCTION READY & LIVE**  
**Custom Domain:** `https://akruti.provaani.xyz` (Valid Let's Encrypt SSL)  
**Inbound Phone Number:** `+91 80-31336640` (`918031336640`)

---

## 1. Executive Summary

The Provaani Akruti Voice AI Receptionist system has completed full production deployment, hardening, telemetry enhancement, mobile UI optimization, and custom domain SSL provisioning on a dedicated Google Cloud Platform VM in Delhi (`asia-south2-b`).

All live patient calls into `+91 80-31336640` are routed directly to the Dograh FastAPI + Pipecat engine, executing low-latency Cerebras `gpt-oss-120b` (~350ms TTFB) and Sarvam AI Indic TTS/STT pipelines.

---

## 2. Infrastructure & Access Credentials

### 🖥️ GCP VM Infrastructure
| Item | Value / Configuration |
| :--- | :--- |
| **Instance Name** | `instance-20260815-072654` |
| **GCP Project** | `project-cb090c10-8c6d-44c8-bbb` |
| **Region / Zone** | `asia-south2-b` (Delhi, India) |
| **Specs** | 2 vCPU, 4 GB RAM, 30 GB SSD |
| **Static External IP** | `34.131.238.156` (Permanently Reserved) |
| **Inbound Phone Number** | `+91 80-31336640` |
| **App Path on VM** | `/home/rahul/Provaani_akruti` |

### 🔑 SSH Access
```bash
# Connect via Google Cloud CLI:
gcloud compute ssh instance-20260815-072654 --zone=asia-south2-b --project=project-cb090c10-8c6d-44c8-bbb
```

### 🌐 Live Public Endpoints & Credentials
| Service | Public URL | Credentials / Notes |
| :--- | :--- | :--- |
| **Secure Client Dashboard** | **`https://akruti.provaani.xyz`** | `admin@provaani.xyz` / `Admin123!` (Org ID: 1) |
| **Direct IP Dashboard** | `http://34.131.238.156:3011` (or `:80`) | Direct port fallback |
| **Dograh Admin UI** | `http://34.131.238.156:3010` | `admin@provaani.xyz` / `Admin123!` |
| **Dograh Backend API** | `http://34.131.238.156:8000` | Health: `/api/v1/health` |
| **Plivo Answer Webhook** | `http://34.131.238.156:8000/api/v1/telephony/inbound/run` | Method: `POST` |
| **Plivo Hangup Webhook** | `""` *(LEAVE EMPTY — Prevents duplicate runs)* | — |

---

## 3. Active Voice AI Pipeline Configuration

```json
{
  "model_configuration_v2_override": {
    "byok": {
      "mode": "pipeline",
      "pipeline": {
        "llm": {
          "provider": "openai",
          "model": "gpt-oss-120b",
          "base_url": "https://api.cerebras.ai/v1",
          "api_key": ["csk-wcpmyy58frvy386y8wjkxv39p2wmne6rkpv284hvfcwktnfj"],
          "temperature": 0.35
        },
        "stt": {
          "provider": "smallest",
          "model": "pulse",
          "language": "north_indic",
          "api_key": ["sk_d341cbfa5e2ad090db0691e1482590d6"],
          "keywords": "blepharoplasty, rhinoplasty, dimpleplasty, buccal fat, gynaecomastia, liposuction, abdominoplasty, tummy tuck, cryolipolysis, micropigmentation, akruti, anand, durgapur, burdwan"
        },
        "tts": {
          "provider": "smallest",
          "model": "lightning_v3.1_pro",
          "voice": "meher",
          "language": "auto",
          "speed": 0.9,
          "api_key": ["sk_d341cbfa5e2ad090db0691e1482590d6"]
        }
      }
    }
  },
  "dictionary": "blepharoplasty, rhinoplasty, dimpleplasty, buccal fat, gynaecomastia, liposuction, abdominoplasty, tummy tuck, cryolipolysis, micropigmentation, akruti, anand, durgapur, burdwan",
  "max_call_duration": 600,
  "max_user_idle_timeout": 30,
  "user_turn_stop_timeout": 0.5
}
```

---

## 4. Key Work Accomplished in this Session

### A. Resolution of Missing Duration & Audio Recordings
1. **Phantom Hangup Call Elimination:** Plivo had its `hangup_url` pointing to `/inbound/run`, which created ghost 0s records (e.g. Run 64) on call disconnect. Cleared `hangup_url` and purged 22 ghost records from PostgreSQL.
2. **Duration Schema Fallback:** Fixed `Dashboard.jsx` to read `r.usage_info.call_duration_seconds || r.cost_info.call_duration_seconds || r.duration` instead of strictly `cost_info` (which is empty on BYOK inbound calls).
3. **MinIO Proxy Route:** Changed `MINIO_PUBLIC_ENDPOINT` to `http://34.131.238.156:3011` / `https://akruti.provaani.xyz` so audio streams (`.wav`) and transcripts (`.txt`) resolve directly through Nginx.
4. **Token Backfill:** Backfilled `public_access_token` for all historical completed calls.

### B. Mobile UI Redesign (Impeccable UI Principles)
1. **Full-Width Audio Player Bar:** Dedicated pill audio player with waveform scrub track, time badge (`-- / 1m 25s`), volume mute, and download button.
2. **Dedicated Transcript Trigger:** Clean full-width button opening the standalone decoupled transcript modal.
3. **2-Column Telemetry Grid:** Added real-time badge strip extracting metrics directly from call execution:
   - ⏱️ **Average TTFB:** (e.g. `360ms`)
   - ⚡ **Latency:** (e.g. `1.58s`)
   - 🗣️ **Detected Language:** (e.g. `Bengali`)
   - 🏥 **Procedure of Interest:** (e.g. `Hair transplant`)
   - 📅 **Booking Requested:** (`Yes` / `No`)
4. **Branding & Tab Favicon:** Set `<title>Provaani</title>` and added the official `/logo.jpg` favicon.

### C. Custom Subdomain & Let's Encrypt SSL
1. Installed **Certbot** on the VM and issued a Let's Encrypt SSL certificate for `akruti.provaani.xyz`.
2. Configured `nginx-spa.conf` on port `443` (HTTP/2 enabled) and mounted `/etc/letsencrypt` into the Docker container.
3. Enabled wildcard `*.provaani.xyz` routing to support future client subdomains out of the box.

### D. New Client Deployment Guide
1. Created **`DEPLOYMENT_GUIDE_NEW_CLIENT.md`** with an automated, reproducible guide to provision fresh VMs for future clients in < 5 minutes.
2. Synchronized git state across local repo and VM.

---

## 5. Live Call Verification (Run 63)

| Metric | Measured Value | Verification Result |
| :--- | :--- | :---: |
| **Caller Number** | `+917044311109` | ✅ Displayed |
| **Call Duration** | **84 seconds** (1m 24s) | ✅ Displayed |
| **Language Detected** | **Bengali** | ✅ 100% Pure Script |
| **Procedure Queried** | **Hair transplant** (post-op recovery) | ✅ Captured in Context |
| **Average LLM TTFB** | **360 ms** | ⚡ Ultra-fast |
| **Audio Recording** | `https://akruti.provaani.xyz/voice-audio/recordings/63.wav` | ✅ Playable |
| **Transcript** | Bengali conversation logged in MinIO | ✅ Verified |
| **Call Termination** | `end_call` tool triggered cleanly | ✅ Verified |

---

## 6. Key Files Reference

- `DEPLOYMENT_GUIDE_NEW_CLIENT.md` — Complete guide for provisioning fresh VMs for new clients.
- `VPS_ACCESS.md` — Credentials, firewall ports, and DNS setup guide.
- `custom-ui/src/pages/Dashboard.jsx` — Enhanced dashboard with telemetry grid and audio player.
- `custom-ui/src/index.css` — Responsive mobile styles and glassmorphism design tokens.
- `custom-ui/nginx-spa.conf` — Port 80 & 443 SSL Nginx configuration.
- `docker-compose.yaml` — Hardened service definitions, isolated ports, and volume mounts.
- `apply_language_lock_and_temp.py` — Calibration script for 500ms turn timeout and language prompt.
- `cleanup_retention.py` — Automated 3:00 AM database and log retention script.

---

## 7. Operational Commands Quick Sheet

```bash
# Restart the entire stack
cd /home/rahul/Provaani_akruti && sudo docker compose restart

# Rebuild and reload Custom UI
sudo docker compose build custom-ui && sudo docker compose up -d custom-ui

# View live API logs
sudo docker logs -f provaani_akruti-api-1 --tail 50

# Check container status & memory
sudo docker ps && free -h

# Check SSL certificate expiration
sudo certbot certificates
```
