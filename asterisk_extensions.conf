[general]
static = yes
writeprotect = no

[from-plivo]
exten => _.,1,NoOp(Incoming SIP call from Plivo Zentrunk: Caller=${CALLERID(num)} to Exten=${EXTEN})
same => n,Ringing()
same => n,Stasis(dograh_app)
same => n,Hangup()

[default]
exten => _.,1,Goto(from-plivo,${EXTEN},1)
